<!-- Main navigation links list -->

<!-- Home -->
<%= nav_tab t('pages.home'), root_path, 
    class: link_class, 
    data: { action: "dropdown#toggle", turbo_frame: '_top'},
    current_page: current_page 
%>

<%= tag.span '|', class: 'text-gray-700 font-bold hidden lg:block'%>

<!-- Gallery -->
<%= nav_tab t('gallery.title'), gallery_path, 
    class: link_class, 
    data: { action: "dropdown#toggle", turbo_frame: '_top'},
    current_page: current_page 
%>

<%= tag.span '|', class: 'text-gray-700 font-bold hidden lg:block'%>

<!-- About -->
<%= nav_tab 'Обо мне', root_path(anchor: 'features'), 
    class: link_class, 
    data: { action: "dropdown#toggle", turbo_frame: '_top'},
    current_page: current_page 
%>

<%= tag.span '|', class: 'text-gray-700 font-bold hidden lg:block'%>

<!-- Contacts -->
<%= nav_tab 'Контакты', root_path(anchor: 'contact_me'), 
    class: link_class, 
    data: { action: "dropdown#toggle", turbo_frame: '_top'},
    current_page: current_page 
%>