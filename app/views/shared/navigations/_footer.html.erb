<!-- Footer -->
<%= provide :footer do %>
    <footer class="bg-gray-200 dark:bg-black border-t dark:border-gray-700 hover:dark:border-gray-600 mt-12">
        <%= link_to '#top', class: 'absolute right-2 bottom-5 z-20', data: {controller: 'scroll-to'} do %>
            <%# image_tag('icons/icons8-up-2-50.png', class: 'w-8 hover:w-10 h-auto')%>
        <% end unless controller_name == 'errors'%>
        <div class="container px-6 py-8 mx-auto">
            <div class="text-center">
                <!-- Header -->
                <div class="relative flex items-center justify-between">
                    <!-- Social links -->
                    <div class="flex items-center justify-start space-x-3 rtl:space-x-reverse">
                        <%= render 'shared/navigations/social_links'%>
                    </div>
                    <!-- Logotype -->
                    <%= link_to root_path, class: "flex justify-center" do %>
                        <%= image_tag 'PhaybiN-logo-white.png', class: 'h-16 lg:h-20 w-auto'%>
                    <% end %>
                    <!-- Locales -->
                    <div class="flex justify-end mr-2">
                        <%= render 'shared/locales'%>
                    </div>
                </div>
            </div>

            <hr class="my-6 dark:border-gray-700 md:my-10" />
            <!-- Copyright -->
            <div class="flex flex-col items-center w-full md:relative">
                <%= link_to 'phaybin@phaybin.com', "mailto:phaybin@phaybin.com", data: { turbo_frame: '_top'},
                    class:"text-gray-500 dark:text-gray-500 hover:dark:text-gray-200 md:absolute md:right-0 hover:underline mb-4 md:mb-0"%>
                <p class="text-gray-500 text-center">© Copyright <%= Time.now.strftime('%Y')%>. All Rights Reserved.</p>
            </div>
        </div>
    </footer>
<% end%>

